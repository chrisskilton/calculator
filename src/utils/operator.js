export const OPERATOR_FUNCS = {
    ADD: (a, b) => parseFloat(a) + parseFloat(b),
    SUBTRACT: (a, b) => parseFloat(a) - parseFloat(b),
    DIVIDE: (a, b) => parseFloat(a) / parseFloat(b),
    MULTIPLY: (a, b) => parseFloat(a) * parseFloat(b),
};